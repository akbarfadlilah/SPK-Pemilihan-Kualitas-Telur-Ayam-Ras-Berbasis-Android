<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar.FullScreen">
        <activity android:name=".MainActivity"
            android:exported="true"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".SawStatisActivity" />
        <activity android:name=".AnalisaSawActivity" />
        <activity android:name=".AlternatifActivity" />
        <activity android:name=".KriteriaActivity" />
        <activity android:name=".AlternatifKriteriaActivity" />
        <activity android:name=".AddAlternatifActivity" />
        <activity android:name=".AddAlternatifKriteriaActivity" />
        <activity android:name=".AddKriteriaActivity" />
        <activity android:name=".EditAlternatifActivity" />
        <activity android:name=".EditKriteriaActivity" />
        <activity android:name=".EditAlternatifKriteriaActivity" />
        <activity android:name=".ChangePasswordActivity"></activity>
    </application>

</manifest>